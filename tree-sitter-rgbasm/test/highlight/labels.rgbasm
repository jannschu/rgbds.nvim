main:
; <- module
;   ^ punctuation.bracket.label
main::
; <- module.builtin
;   ^ punctuation.delimiter
main.local
; ^ label

main.local:
; ^ label
;         ^ punctuation.bracket

.local
; <- label
.foo:
; <- label
;   ^ punctuation.bracket
:
; <- label

main\@:
; <- module
;   ^^ punctuation.special
;     ^ punctuation.bracket.label
.local\@:
; <- label
;     ^^punctuation.special
;       ^ punctuation.bracket
.local\@
; <- label
;     ^^ punctuation.special
