main:
  add a, b
  ; <- function.call
  ;   ^ variable.builtin
  ;    ^ punctuation.delimiter
  ;      ^ variable.builtin

  jr nc, :+
  ; <- function.call
  ;  ^^ keyword.conditional
  ;    ^ punctuation.delimiter
  ;      ^^ label

  ld hl, $1234
  ; <- function.call
  ;  ^^ variable.builtin
  ;    ^ punctuation.delimiter
  ;      ^^^^^ number

  ldi a, [hl]
  ; <- function.call
  ;   ^ variable.builtin
  ;    ^ punctuation.delimiter
  ;      ^ punctuation.bracket
  ;       ^ variable.builtin
  ;         ^ punctuation.bracket

